Options -MultiViews
RewriteEngine On

# Ignorer les fichiers/dossiers existants (assets, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout rediriger vers index.php
RewriteRule ^ index.php [QSA,L]
